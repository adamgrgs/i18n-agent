[project]
name = "multilingual-i18n-seo-skill"
version = "1.0.0"
description = "Framework-agnostic multilingual i18n + SEO checker scripts for the AI skill package."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
