# Sample audit report

Generated by running the skill checker against the included HTML fixtures.

## Pass fixtures

```bash
python3 scripts/check_i18n_seo.py \
  --root tests/fixtures/html/pass \
  --registry templates/locale-registry.example.json \
  --sitemap tests/fixtures/sitemaps/sitemap-pass.xml \
  --format both \
  --fail-on error
```

### Text output

```text
Scanned pages: 3
Findings: 0 (errors=0, warnings=0)
```

### JSON output

```json
{
  "version": "1.0.0",
  "pagesScanned": 3,
  "summary": {
    "errors": 0,
    "warnings": 0,
    "info": 0
  },
  "findings": []
}
```

## Fail samples (selected)

### hreflang on noindex

```text
[ERROR] hreflang_on_noindex @ en/draft.html: hreflang alternates must not be present on noindex pages
[ERROR] og_locale_alternate_on_noindex @ en/draft.html: og:locale:alternate must not be present on noindex pages
```

### Slug-cased BCP47 (`fr-ca` vs `fr-CA`)

```text
[ERROR] hreflang_bcp47_case @ fr/pricing.html: hreflang 'fr-ca' should be 'fr-CA' per registry
[ERROR] hreflang_slug_case @ fr/pricing.html: hreflang 'fr-ca' looks slug-cased; expected BCP47 'fr-CA'
```

### og:url ≠ canonical

```text
[ERROR] og_url_mismatch @ en/pricing.html: og:url does not equal canonical | canonical=https://example.com/en/pricing og:url=https://example.com/en/other
```

### Missing hreflang on indexable page

```text
[ERROR] missing_hreflang @ en/pricing.html: Indexable page missing hreflang alternates
```

### html[lang] vs path locale

```text
[ERROR] html_lang_mismatch @ fr/pricing.html: html[lang]='en' does not match registry bcp47='fr-CA'
```

## Registry validation

```bash
python3 scripts/validate_locale_registry.py templates/locale-registry.example.json
# OK: templates/locale-registry.example.json
```

## Tests

```bash
python3 -m pytest tests/ -q
# 9 passed
```
